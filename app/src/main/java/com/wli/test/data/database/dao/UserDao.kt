package com.wli.test.data.database.dao

import androidx.room.Dao

/**
 * Data Access Object interface to perform database operations
 */
@Dao
interface UserDao {

  /*  @Query("Select * from TennisPlayers")
    fun getAllPlayer(): List<Player>

    @Insert(onConflict = OnConflictStrategy.REPLACE)
    fun insertPlayers(list: List<Player>?)

    @Query("DELETE FROM TennisPlayers")
    fun deleteAll()*/

}